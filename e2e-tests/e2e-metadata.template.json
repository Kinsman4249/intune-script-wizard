{
  "answers": {
    "confirmDevTenant": false,
    "runPrefix": "ZZZ-E2E-TEST-DELETE-ME",
    "notes": "This kit generates scripts that New-E2ETestSet.ps1 does NOT deploy itself - you run Deploy-IntuneScripts.ps1 against the generated folders yourself, against a DEV tenant. confirmDevTenant is a safety gate: New-E2ETestSet.ps1 refuses to generate anything until you set it to true, as confirmation that you understand this and that -Path in your own Deploy-IntuneScripts.ps1 run will point at a dev tenant's script folder, not production. runPrefix is stamped on every generated script's Intune display name AND is what Remove-E2ETestSet.ps1 matches on to clean up afterwards - keep it deliberately loud (the ZZZ- sorts it to the bottom of an alphabetical script list too) so leftover test scripts are obvious at a glance if cleanup is ever skipped or fails. Bump it (e.g. ZZZ-E2E-TEST-DELETE-ME-2) to start a fresh, non-colliding batch instead of updating the previous one - just remember Remove-E2ETestSet.ps1 only matches the CURRENT runPrefix, so clean up the old batch under its own prefix before changing this."
  },
  "groups": {
    "include": {
      "displayName": "",
      "guid": "",
      "notes": "Fill in displayName to generate device/20-group-by-name.ps1, guid to generate device/21-group-by-guid.ps1; fill in both to get both - this is the only role where the two forms produce separate tests. Everything else that needs an include group uses displayName only (device/22-group-multiple.ps1, 24-group-and-excludegroup.ps1, 25-kitchen-sink.ps1), so a guid on its own generates test 21 and nothing more. Leave both blank and every include test is skipped (noted in CHECKLIST.md)."
    },
    "includeSecondary": {
      "displayName": "",
      "guid": "",
      "notes": "A second group, assigned alongside 'include' by the multi-#group: test. That test uses display names, so only displayName is read here."
    },
    "exclude": {
      "displayName": "",
      "guid": "",
      "notes": "Used by the #excludegroup: tests: device/23-excludegroup-only.ps1 (everyone except this group), plus device/24-group-and-excludegroup.ps1 and device/25-kitchen-sink.ps1, which also need include.displayName. Only ONE form is ever generated - displayName wins and the guid is ignored if both are filled in, so fill in guid ALONE to exercise the bare-guid form. Leave both blank and all three tests are skipped (noted in CHECKLIST.md)."
    }
  }
}
