{
  "answers": {
    "confirmDevTenant": false,
    "runPrefix": "ZZZ-E2E-TEST-DELETE-ME",
    "notes": "This kit generates scripts that New-E2ETestSet.ps1 does NOT deploy itself - you run Deploy-IntuneScripts.ps1 against the generated folders yourself, against a DEV tenant. confirmDevTenant is a safety gate: New-E2ETestSet.ps1 refuses to generate anything until you set it to true, as confirmation that you understand this and that -Path in your own Deploy-IntuneScripts.ps1 run will point at a dev tenant's script folder, not production. runPrefix is stamped on every generated script's Intune display name AND is what Remove-E2ETestSet.ps1 matches on to clean up afterwards - keep it deliberately loud (the ZZZ- sorts it to the bottom of an alphabetical script list too) so leftover test scripts are obvious at a glance if cleanup is ever skipped or fails. Bump it (e.g. ZZZ-E2E-TEST-DELETE-ME-2) to start a fresh, non-colliding batch instead of updating the previous one - just remember Remove-E2ETestSet.ps1 only matches the CURRENT runPrefix, so clean up the old batch under its own prefix before changing this."
  },
  "groups": {
    "include": {
      "displayName": "",
      "guid": "",
      "notes": "Fill in displayName to generate device/20-group-by-name.ps1, guid to generate device/21-group-by-guid.ps1; fill in both to get both. Everything else that needs an include group uses displayName only (device/22-group-multiple.ps1, 24-group-and-excludegroup.ps1, 25-kitchen-sink.ps1), so a guid on its own generates test 21 and nothing more. Leave both blank and every include test is skipped (noted in CHECKLIST.md)."
    },
    "includeSecondary": {
      "displayName": "",
      "guid": "",
      "notes": "A second group, assigned alongside 'include' by the multi-#group: test. That test uses display names, so only displayName is read here."
    },
    "exclude": {
      "displayName": "",
      "guid": "",
      "notes": "The two forms are independent tests, not alternatives, and they do NOT have to be the same group - fill in both and both run, whether they name one group or two. displayName generates device/23-excludegroup-only.ps1 and (with include.displayName) device/24-group-and-excludegroup.ps1; guid generates device/23b-excludegroup-by-guid.ps1 and (with include.displayName) device/24b-group-and-excludegroup-by-guid.ps1, which are the only tests covering an exclusion that skips the directory lookup. device/25-kitchen-sink.ps1 is about stacking directives rather than reference forms, so it takes the display name if there is one and the guid otherwise, and is generated once either way. Leave both blank and every exclude test is skipped (noted in CHECKLIST.md)."
    },
    "sameGroupBothForms": {
      "displayName": "",
      "guid": "",
      "notes": "ONE group, written both ways: the displayName and the guid here must point at the same group, which is what makes this role different from every other one. Any group will do, including one already used above. It feeds a single test, expect-failure/device/group-ref-name-guid-clash.ps1, which includes the group by name and excludes it by guid: two different strings that only turn out to be the same group once Graph has resolved them, so the wizard's parse-time overlap check cannot catch it and the post-resolution check has to. Leave either field blank and that test is skipped (noted in CHECKLIST.md)."
    }
  }
}
